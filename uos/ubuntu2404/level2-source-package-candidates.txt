# Level 2 Ubuntu 24.04 Source Package Candidates

# Start with small dependency packages before CPython. Record exact Ubuntu source
# versions, patches, configure flags, toolchain path, and runtime result before
# treating any package as passed.

zlib # passed first K1OM rebuild/runtime proof from Ubuntu 24.04 Noble source
libffi # blocked: Ubuntu source requires Autoconf 2.71+, newer than current host
ncurses # passed simple K1OM library/runtime proof; setupterm still needs work
readline
sqlite3 # blocked: Ubuntu source-package build needs host tclsh
openssl
xz-utils
bzip2
expat
gdbm
