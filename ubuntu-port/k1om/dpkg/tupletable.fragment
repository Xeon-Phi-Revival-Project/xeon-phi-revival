# Experimental dpkg tupletable note for K1OM.
# This is not upstream dpkg metadata.
#
# dpkg 1.22's generic mapping already resolves:
#
#   base-gnu-linux-<cpu>    <cpu>
#
# Once cputable contains k1om, no explicit tupletable row is required.
